{
  "name": "vialert",
  "version": "0.1.0",
  "private": true,
  "description": "VIALERT emergency mobility simulation and traffic operations demo",
  "engines": { "node": ">=22.12.0" },
  "workspaces": ["frontend", "backend/server-node"],
  "scripts": {
    "dev": "concurrently --kill-others --names web,node,ai \"npm:dev:frontend\" \"npm:dev:node\" \"npm:dev:ai\"",
    "dev:frontend": "npm run dev --workspace=@vialert/frontend",
    "dev:client": "npm run dev:frontend",
    "dev:node": "npm run dev --workspace=@vialert/server-node",
    "dev:ai": "uv run --project backend/server-ai uvicorn app.main:app --app-dir backend/server-ai --reload --host 127.0.0.1 --port 8000",
    "setup:ai": "uv sync --project backend/server-ai --locked",
    "build": "npm run build --workspace=@vialert/frontend",
    "test:data": "node --test scripts/data.test.mjs",
    "test:frontend": "npm test --workspace=@vialert/frontend",
    "test:client": "npm run test:frontend",
    "test:node": "npm test --workspace=@vialert/server-node",
    "test:ai": "uv run --project backend/server-ai pytest backend/server-ai/tests",
    "test": "npm run test:data && npm run test:frontend && npm run test:node && npm run test:ai",
    "check": "npm run build && npm test",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": { "concurrently": "10.0.5" }
}
