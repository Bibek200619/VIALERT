# Map components

This directory began as the Phase 1 shared-map placeholder. The live map
implementations now live with their workspaces: `features/ambulance`,
`features/traffic`, and `features/simulation`, each with a Leaflet/graph fallback.
Keep this note as a pointer rather than treating the placeholder directory as a
second map implementation.
