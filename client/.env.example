# Leave empty to use the Vite /api proxy (Node at 127.0.0.1:4000).
VITE_API_BASE_URL=
# Vite strips /ai and forwards to FastAPI at 127.0.0.1:8000.
VITE_AI_BASE_URL=/ai
# Optional compatible Leaflet tile URL; defaults to standard OpenStreetMap raster tiles.
VITE_OSM_TILE_URL=
