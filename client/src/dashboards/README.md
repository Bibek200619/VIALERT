# Legacy dashboard wrappers

The files under this directory are the original Phase 1 placeholder wrappers.
They are retained only for historical context and are not imported by the live
router. Current screens are route-level pages in `client/src/pages/` with feature
logic under `client/src/features/`. Do not add new dashboard behavior here.
